#include <iostream>

using namespace std;

int main() {
  int a, b, c;
  char i = 127 + 1;

  //cout << "largest = " << max(max(a, b), c) << endl;
  //cout << "smallest = " << min(min(a, b), c) << endl;
  cout << int(i);
  return 0;
}